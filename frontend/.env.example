# Copy this to .env.local and adjust for your environment.

# Base URL of the backend API. Defaults to http://localhost:8000 for
# local dev if unset — only needs to be set when deploying the frontend
# somewhere other than localhost (e.g. pointing a hosted frontend at a
# separately hosted backend).
NEXT_PUBLIC_API_URL=http://localhost:8000
